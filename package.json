{
  "scripts": {
    "dev": "npx vuepress dev site",
    "build": "npx vuepress build site --dest docs",
    "deploy": "npm run build && git add . && git commit -m \"deploy\" && git push"
  },
  "devDependencies": {
    "@fortawesome/fontawesome-free": "5.9.0",
    "@vuepress/plugin-google-analytics": "1.0.4",
    "bootstrap": "4.3.1",
    "date-fns": "1.30.1",
    "node-sass": "4.13.1",
    "sass-loader": "7.1.0",
    "vue-goodshare": "^1.3.2",
    "vuepress": "1.0.2",
    "vuepress-plugin-seo": "0.1.2"
  }
}
